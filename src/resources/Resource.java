package resources;
/*
 * We should probably make resources into an enum, then we can do a map for total resources
 * as {particular enum} -> int (number of resource)
 * 
 * Tiles might be better as enums as well, but I'm unsure.
 */
public abstract class Resource {

}
